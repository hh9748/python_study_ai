while
条件驱动循环（只要条件为 True 就继续循环）
写法灵活，尤其适合处理不确定循环次数的场景。
需要手动维护循环条件，维护不好容易死循环。
for
迭代驱动循环（遍历一个可迭代对象的每一项）
语法简洁，处理可迭代对象的首选，不易发生死循环。
只适合一开始就知道要循环几次的场景。

示例说明：
while 提示用户输入密码，若密码不正确就反复提示，直到密码正确。
for 遍历字符串 'Python'，逐个输出每个字符。
while 让用户不断输入数字，直到输入的是一个偶数为止。
for 让用户依次输入 5 个名字，最终把5个名字拼接到一起。